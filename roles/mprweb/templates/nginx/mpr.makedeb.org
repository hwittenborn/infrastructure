server {
  server_name mpr.makedeb.org;
  listen 443 ssl;
  location / {
    proxy_pass http://localhost:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  ssl_certificate /etc/letsencrypt/live/mpr.makedeb.org/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/mpr.makedeb.org/privkey.pem;
}
